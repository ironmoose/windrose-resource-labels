# Handoff: numbered diagnostic atlas + MI-parent feasibility (Windows side) -- 2026-08-04

Answers **both** open items in `CLAUDE.md` "The plan":

- **Step 2 (numbered diagnostic atlas): COOKED, verified, ready to deploy.**
- **The OPEN FEASIBILITY QUESTION: answered, with a hard NO and a promising
  alternate route** -- an MI parented to a *cooked* material authors fine in the
  editor but **crashes the cooker**. A stub-parent MI cooks cleanly and carries
  the override. Details in "Feasibility" below.

Windrose is **not installed on this Windows box** (the old `D:\SteamLibrary` is
gone -- that disk is Fedora now), so nothing here was tested against the real
`M_DD_PlaqueSign`. Everything below is stock-UE-5.6.1 behaviour reproduced with
a locally cooked stand-in material. See `[STOP]` items.

---

## Part 1 -- the diagnostic atlas (deliverable)

### Cook result, verbatim

```
LogInit: Display: Success - 0 error(s), 0 warning(s)
```

### Cooked output (on the Windows partition, NOT committed)

```
C:\WindroseIcons\Saved\Cooked\Windows\WindroseIcons\Content\Environment\Shaders\Textures\Trim\Building\T_PlaqueSign_01_M.uasset   (1086 bytes)
C:\WindroseIcons\Saved\Cooked\Windows\WindroseIcons\Content\Environment\Shaders\Textures\Trim\Building\T_PlaqueSign_01_M.uexp     (940 bytes)
C:\WindroseIcons\Saved\Cooked\Windows\WindroseIcons\Content\Environment\Shaders\Textures\Trim\Building\T_PlaqueSign_01_M.ubulk    (1636904 bytes)
```

From Fedora with the partition mounted: `/mnt/windows/WindroseIcons/Saved/Cooked/Windows/WindroseIcons/Content/Environment/Shaders/Textures/Trim/Building/`.
**Move all three together** (VT pixel data lives in `.ubulk`). Full shutdown
(hold Shift + Shut down) before booting Fedora.

Sizes match the 2026-08-03 content atlas exactly because dimensions and format
are unchanged -- only the pixels differ. The final cook was re-run from scratch
and its bytes are identical to the verified mid-session cook.

### Self-verification (read straight from `verify_engraving_diag_results.txt`)

```
PASS: width (power-of-two, 16 cells x 128px): expected 2048, got 2048
PASS: height (8 rows x 128px): expected 1024, got 1024
PASS: virtual_texture_streaming: expected True, got True
PASS: srgb: expected False, got False
PASS: cooked file ...\T_PlaqueSign_01_M.uasset exists (size=1086)
PASS: cooked file ...\T_PlaqueSign_01_M.uexp  exists (size=940)
PASS: cooked file ...\T_PlaqueSign_01_M.ubulk exists (size=1636904)
```

All PASS -- no cosmetic-FAIL caveat this time (the verify script now expects the
correct 2048 native width).

### What is in it, and how to read a sign

Source: `tools/cook-kit/engraving-test/T_PlaqueSign_01_M_diag.png`, generated by
the committed `tools/cook-kit/engraving-test/gen_diagnostic_atlas.py`.
2048x1024, mode `L`, white on black, 16 cols x 8 rows of 128px cells -- the same
geometry as the proven VT cook, so the VT-ness is not in question.

**Every cell is stamped with its own PHYSICAL address, COLUMN FIRST:**

```
"<col in hex 0-9,A-F><row 0-7>"     e.g.  "60" = physical column 6, row 0
                                          "A2" = physical column 10, row 2
```

Plus, per cell:
- a **4px border** inset from the cell edge, and
- a **solid square in the cell's TOP-LEFT corner**.

Those two are the real measuring instruments:

| What the user sees on a sign | What it means |
|---|---|
| one clean bordered box, two crisp glyphs | the material samples exactly one physical cell -- read the glyphs to get its (col,row) |
| glyphs sliced, two partial boxes side by side | horizontal cell size is wrong (material still dividing by 10 on a 2048-wide texture, or similar) |
| glyphs sliced top/bottom | vertical cell size is wrong |
| corner square appears at the **bottom**-left | UV is V-flipped |
| a whole grid of tiny numbers on one sign | the material is sampling far more than one cell |

**The key measurement.** Place a vanilla sign of a KNOWN index (Ore = index 6
per `CLAUDE.md`) and read it:

- clean **`60`** -> physical column 6, row 0. Index-decode is 10-wide as
  documented AND the material's horizontal cell size already matches the
  2048-wide (16-cell) texture. Route X geometry confirmed.
- sliced glyphs straddling **`90`/`A0`** -> the material is still using a
  horizontal cell size of 1/10 against a 2048-wide texture (u = 6/10 = 0.6 lands
  at x = 1228, i.e. column 9.6). That is the exact mis-match that garbled every
  sign last time, and it is fixable in the material, not in the atlas.
- anything else -> record the literal glyphs; the pair (col,row) directly gives
  the mapping the material actually applies.

**Row growth** (does the grid address rows 2-7?) is read the same way: whatever
mechanism can request a high index, the answer is just whichever row digit shows
up. No inference needed.

**This atlas is a measuring tool, not shippable.** Unlike the earlier test
atlas, rows 0-1 are NOT preserved vanilla art -- every cell is a number, so
every sign in the world (vanilla ones included) will read as numbers while it is
deployed. Local client only. Restore golden afterwards.

---

## Part 2 -- Feasibility: can we author an MI of the cooked `M_DD_PlaqueSign`?

`CLAUDE.md` asks three sub-questions. Answers:

### (2) Official R5 modkit / editor from Kraken Express: **NO**

Windrose (Early Access, April 2026) ships **no official mod support, no Steam
Workshop, and no modding tools or SDK** from Kraken Express. Modding is entirely
community-driven -- hand-built UE `.pak`/`.ucas`/`.utoc` files distributed off
Nexus. So there is no game-specific editor build that would hand us the real
source materials. That unlock is closed for now; worth re-checking if Kraken
Express announces mod support.

### (1) Can the editor load a cooked material and parent an MI to it?

Windrose is not installed here, so this was reproduced locally with an identical
setup: author a material with a `Sign Index Override` scalar, cook it, drop the
**cooked** `.uasset` + `.uexp` back into `Content/` at the same package path
(exactly what dropping in an extracted `M_DD_PlaqueSign` would look like), then
try to build an MI on it.

**Result, in order:**

1. **Plain `load_asset` fails**, and the engine names its own escape hatch:
   ```
   Unable to load package '/Game/CookProbe/M_CookProbe'. Package contains cooked
   data which is not supported by the current build. Enable 'Allow Cooked
   Content In The Editor' in Project Settings under 'Engine - Cooker' section.
   ```
   That is CVar `cook.AllowCookedDataInEditorBuilds`
   (`UCookerSettings::bAllowCookedDataInEditorBuilds`).

2. **With the CVar on, the cooked material loads fine and is introspectable:**
   ```
   OK | load_asset(cooked M_CookProbe) -> <Object '/Game/CookProbe/M_CookProbe' Class 'Material'>
   OK | get_scalar_parameter_names on cooked material -> ['Sign Index Override']
   ```

3. **An MI can be authored on it, and the override sticks and saves:**
   ```
   OK | create MI (no parent) -> <Object '.../MI_CookProbe_Test' Class 'MaterialInstanceConstant'>
   OK | set_material_instance_parent(cooked material)
   OK | read back MI parent -> <Object '/Game/CookProbe/M_CookProbe' Class 'Material'>
   OK | set scalar 'Sign Index Override' = 6.0
   OK | read back scalar -> 6.0
   OK | save MI -> True
   ```

4. **But COOKING that MI crashes the cooker.** Hard assertion, not a warning:
   ```
   Assertion failed: EditorEntry.EditorInfo.Num() == Entry.ParameterInfoSet.Num()
   [File: Engine\Source\Runtime\Engine\Private\Materials\MaterialCachedData.cpp] [Line: 766]
   ...
   LogInit: Display: Failure - 1 error(s), 4 warning(s)
   ```
   The cooked parent has no editor-only parameter info, so the cached-parameter
   bookkeeping mismatches the moment the cooker touches it. The crash killed the
   cook mid-run and **deleted the already-cooked atlas output** on its way down
   (recovered from backup, then re-cooked clean -- see "State left").

**Verdict: parenting a production MI directly to the extracted cooked material
is NOT viable on stock UE 5.6.1.** It works right up to the cook and then dies.

### The alternate route that DOES cook: a stub parent

Standard UE modding workaround, and it works here: parent the MI to a **stub**
material -- one we author ourselves in the editor, at the **same package path**,
carrying the **same parameter names** as the real one. The MI then cooks through
the completely normal path; at runtime the game resolves the import by package
path to **its own** real material, and scalar overrides bind **by name**.

Cook result with the stub parent:

```
LogInit: Display: Success - 0 error(s), 0 warning(s)
```

And the cooked MI genuinely carries what we need -- strings and bytes read back
out of the cooked files:

```
MI_CookProbe_Test.uasset -> ['/Game/CookProbe/MI_CookProbe_Test', 'EMaterialParameterAssociation',
  'MaterialInstanceBasePropertyOverrides', 'MaterialParameterInfo', 'ScalarParameterValue',
  'ScalarParameterValues', 'Sign Index Override', '/Game/CookProbe/M_CookProbe', ...]
MI_CookProbe_Test.uexp   -> contains float 6.0 bytes: True
```

Parent referenced by package path, `Sign Index Override` in
`ScalarParameterValues`, value `6.0` serialized. That is the whole payload a
per-index override MI needs.

**What this does and does not prove.** It proves the *cook* side is unblocked
and the MI serializes correctly. It does **not** prove the runtime binds our MI
to the game's real material and honours the override -- that is an in-game
question only Fedora can answer, and it is the natural content of the Step 1
test that `CLAUDE.md` already describes.

**Note for packing:** the cook emits the stub's own cooked `M_CookProbe.uasset`/
`.uexp` alongside the MI. **Ship only the MI**; the stub's cooked output must be
discarded, never packed, or it would override the game's real material with an
empty one.

---

## `[STOP]` -- ambiguities and corrections, recorded not guessed

1. **`CLAUDE.md`'s Python sketch does not run on UE 5.6.1.**
   `factory.set_editor_property("initial_parent", parent)` fails:
   ```
   MaterialInstanceConstantFactoryNew: Failed to find property 'initial_parent'
   ```
   `UMaterialInstanceConstantFactoryNew::InitialParent` is a bare `UPROPERTY()`
   and is not exposed to Python. **Working pattern:** create the MI with a bare
   factory, then
   `unreal.MaterialEditingLibrary.set_material_instance_parent(mi, parent)`
   (fallback `mi.set_editor_property("parent", parent)` -- `UMaterialInstance::Parent`
   is `EditAnywhere`, so it also works). The sketch in `CLAUDE.md` has been
   corrected in place.

2. **`set_material_instance_scalar_parameter_value` returns `False` even on
   success.** It returned `False` while `get_..._scalar_parameter_value` read
   back `6.0` and the value serialized into the cooked `.uexp`. **Do not treat
   its return value as a success signal** -- read the parameter back instead.

3. **The parent material's package path is UNVERIFIED from Windows.**
   `CLAUDE.md`'s sketch uses `/Game/Environment/Shaders/Decal/M_DD_PlaqueSign`.
   With the game not installed here I cannot confirm it. A stub must sit at the
   **exact** vanilla path or it overrides nothing (prime directive). **Fedora:
   send the exact package path from the retoc extraction.**

4. **The parameter name `Sign Index Override` is UNVERIFIED from Windows.** I
   used it because `CLAUDE.md` names it, and a stub's parameter names must match
   the real material's **exactly** (runtime binds by name). **Fedora: confirm the
   literal parameter name from the extracted material.**

5. **The feasibility test used a material cooked by stock UE 5.6.1, not by the
   R5 fork.** The load/crash behaviour is engine-generic, so I expect it to hold
   -- but a fork-cooked package could differ (custom versions, shader maps). To
   close this properly I need the extracted cooked `M_DD_PlaqueSign` (and any
   material functions it imports: `MF_UDIMIndexSelect`,
   `MF_IndexedPaletteUVOffset`, `MF_CPDOverride`) copied onto the Windows
   partition. **That is my main ask.** With those I can re-run the exact probes
   against the real asset and, if the stub route holds, mint the Step 1 MI.

6. **The `[/Script/UnrealEd.CookerSettings]` ini section did NOT take effect for
   the cook commandlet.** Setting `bAllowCookedDataInEditorBuilds=True` there was
   silently ignored (the cook still printed the "enable it in Project Settings"
   error). Only two things worked: `execute_console_command("cook.AllowCookedDataInEditorBuilds 1")`
   from Python, and a `[ConsoleVariables]` block in `DefaultEngine.ini`. Both ini
   edits have since been reverted -- see below.

---

## What Fedora should do next

1. **Deploy the diagnostic atlas** (three files above) as a plain texture
   override, local client only, per `docs/pack-engraving-atlas-fedora.md`. Have
   the user place/inspect vanilla signs of known index and **report the literal
   glyphs** (e.g. "clean `60`" / "sliced, half a `9` and half an `A`"). That
   measures the true geometry outright -- no inference. Restore golden after.
2. **Send back over the mount** the extracted cooked `M_DD_PlaqueSign` + its
   material functions, plus the confirmed package path and parameter name
   (`[STOP]` 3-5).
3. Then I can run the real-asset probe and, if the stub route survives it, cook
   the Step 1 override MI (`Sign Index Override` = 6 at
   `/Game/Environment/Shaders/InstanceMaterials/Decal/PlaqueSign/MI_DD_PlaqueSign_01`)
   for the in-game persistence test.
4. The byte-patch fallback (`tools/uparse.py`, adding a scalar entry to the
   existing `MI_DD_PlaqueSign_01`) is **not** needed yet -- the stub route is
   cheaper and cooks clean. Keep it in reserve for if the runtime rejects a
   stub-parented MI.

---

## State left on the Windows machine

- **Deliverable:** the three cooked atlas files above (2026-08-04, from a final
  clean `Success - 0 error(s), 0 warning(s)` cook, byte-identical to the
  mid-session verified cook). Not committed.
- **Backups** (in case anything is still wanted):
  - `C:\WindroseIcons\Saved\_prior_cook_2048_content_backup\` -- the 2026-08-03
    *content* atlas cook (the 16-col left-aligned one).
  - `C:\WindroseIcons\Saved\_diag_cook_backup\` -- copy of this diagnostic cook.
  - `C:\WindroseIcons\Saved\_prior_test_cook_1280x384_backup\` -- older, from the
    previous session.
- **Imported asset** `/Game/.../Building/T_PlaqueSign_01_M` is now the
  **diagnostic** 2048x1024 VT. To go back to the content atlas, re-run
  `import_engraving_atlas.py` (its source PNG is still in `SourceIcons\`).
- **Project config is back to its known-good state.** Both temporary ini edits
  were reverted: `+DirectoriesToAlwaysCook=(Path="/Game/CookProbe")` removed from
  `DefaultGame.ini`; the `CookerSettings` and `[ConsoleVariables]` blocks removed
  from `DefaultEngine.ini`. Verified: zero remaining references.
- **Probe assets deleted:** `Content/CookProbe/` and its cooked output are gone,
  so the next cook is clean. Throwaway scripts left in `C:\WindroseIcons\`, not
  committed: `import_engraving_diag.py`, `verify_engraving_diag.py`,
  `probe_step1_make_material.py`, `probe_step2_mi_from_cooked.py`,
  `probe_step3_mi_from_cooked_allowed.py`, `probe_step4_mi_parent_setter.py`,
  `probe_step5_stub_parent.py`, and their `*_results.txt`.
- **Committed to the repo:** `tools/cook-kit/engraving-test/gen_diagnostic_atlas.py`
  and `tools/cook-kit/engraving-test/T_PlaqueSign_01_M_diag.png` (source art, not
  a binary build product), plus this handoff.
